Don't run the train_target_models_and_save.py unless you want to overwrite the .h5 file and the .json file (or change the name in the file first)

The current accuracy of the model on testing data is 66.38%, loss is 7.59